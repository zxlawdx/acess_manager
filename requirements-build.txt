-r requirements.txt

# Build reproduzível - mesmas versões/base usadas no PIBIC LAB.
pyinstaller==6.22.2
pywebview==6.2.1

# Backend Windows do pywebview.
# Qt 6 traz um QtWebEngine/Chromium mais novo, evitando diferenças de
# compatibilidade de CSS/JS observadas no bundle com Qt 5.
PyQt6>=6.7,<7
PyQt6-WebEngine>=6.7,<7
qtpy>=2.4,<3

# Dependências auxiliares que o PyInstaller/setuptools pode carregar
# dinamicamente durante a análise do bundle.
jaraco.text
jaraco.functools
jaraco.context
more-itertools
